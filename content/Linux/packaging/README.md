---
title: Linux Packaging
description: Linux package management — apt, pacman, yum/dnf, dpkg, repositories, AUR, COPR
tags:
  - linux
---

# Linux Packaging

- [[Linux/packaging/apt|apt]] — Debian/Ubuntu package manager, `apt-get`, `apt-cache`, sources.list
- [[Linux/packaging/pacman|pacman]] — Arch/Manjaro, `-S`/`-R`/`-U`, ABS, AUR helpers
- [[Linux/packaging/yum-dnf|yum/dnf]] — RHEL/Fedora, `yum` vs `dnf`, repos, `dnf` plugins
- [[Linux/packaging/dpkg|dpkg]] — low-level `.deb` tool, `dpkg -i`, `dpkg -l`, `/var/lib/dpkg`
- [[Linux/packaging/package-repos|Package Repositories]] — sources.list, COPR, AUR, PPA, creating local repos